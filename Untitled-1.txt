to apply devops principles(ci/cd,collaboration) to a sample product development workflow by a setup a git repositiory,integrate code changes and demonstrate automated testing.
from app.main import add

def test_add():
    assert add(2, 3) == 5
ChatGPT said:

Below is a simple, end-to-end DevOps-style product development workflow using Git, CI/CD, collaboration practices, and automated testing—using your sample add() function and test.

✅ 1. Project Structure
my-app/
│
├── app/
│   ├── __init__.py
│   └── main.py        # contains add()
│
├── tests/
│   └── test_main.py   # contains test_add()
│
├── requirements.txt
└── .github/
    └── workflows/
        └── ci.yml     # GitHub Actions CI pipeline

✅ 2. Application Code (app/main.py)
def add(a, b):
    return a + b

✅ 3. Automated Test (tests/test_main.py)

You already provided this:

from app.main import add

def test_add():
    assert add(2, 3) == 5

✅ 4. Initialize a Git Repository

In your project folder:

git init
git add .
git commit -m "Initial commit with app and tests"


Add a remote (GitHub):

git remote add origin https://github.com/yourusername/my-app.git
git push -u origin main

✅ 5. Set Up CI/CD (GitHub Actions Example)

Create file:

.github/workflows/ci.yml


Contents:

name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest -v


This pipeline:

Runs on every push and pull request

Installs dependencies

Runs automated tests

Fails the workflow if tests fail

✅ 6. Collaboration Workflow (Git Flow)
Developer A creates a feature branch
git checkout -b feature/add-function

Makes changes → Commits → Pushes
git add .
git commit -m "Implemented add() function"
git push -u origin feature/add-function

Opens a Pull Request (PR)

Team members review before merging.

CI Runs Automatically

If test_add() fails → PR is blocked.

After approval
git merge feature/add-function

✅ 7. Automated Testing in Action

Example output from GitHub Actions:

=================== test session starts ===================
collected 1 item

tests/test_main.py::test_add PASSED                   [100%]

==================== 1 passed in 0.02s ====================


If someone introduces a bug:

def add(a, b):
    return a - b   # wrong


CI fails, preventing broken code from being merged.

✅ 8. Optional CD Step (e.g., Deploy to Server)

You could extend ci.yml:

- name: Deploy to server
  run: ./deploy.sh


Or use GitHub Pages, AWS, Docker, etc.

🎉 Outcome

Using this setup, you’ve demonstrated:

✔ Continuous Integration

Tests run automatically on every commit.

✔ Collaboration

Developers work via branches + pull requests.

✔ Automated Testing

pytest validates functionality.

✔ CI/CD Pipeline

GitHub Actions automates the workflow end-to-end.